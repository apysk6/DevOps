sudo: required

services:
    - docker

before_install:
    - docker build -t apysk6/devops/mydockerfrontend -f Dockerfile.dev .

script:
    - docker run -e  CI=true apysk6/devops/mydockerfrontend npm test